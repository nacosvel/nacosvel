name: subtree-matrix

on: [ push ]

permissions: write-all

jobs:
  sync-downstream:

    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        path:
          - composer
          - console
          - container-interop

    name: Update downstream ${{ matrix.path }} package

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: nxtlvlsoftware/git-subtree-action@1.1
        with:
          deploy_key: ${{ secrets.DEPLOY_BOT_PUBLIC_KEY }}
          repo: 'git@github.com:nacosvel/${{ matrix.path }}.git'
          path: '${{ matrix.path }}'
          force: true
